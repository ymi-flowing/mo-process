playwright>=1.44
python-docx>=1.1
pypdf>=4.0
requests>=2.31
# docx2pdf is optional and Windows/Mac only (needs MS Word). On Linux CI
# the runner uses `soffice --headless --convert-to pdf` (LibreOffice) instead.
docx2pdf>=0.1; platform_system == "Windows" or platform_system == "Darwin"
